let initialState = {
    name: "NETS CRM for tovarka",
    telephone: "099-056-7443",
    email: "yaratechsoftware@gmail.com",
}

const firmReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default firmReducer;